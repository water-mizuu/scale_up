import "package:flutter/material.dart";

const defaultBoxShadow = [
  // BoxShadow(color: Color(0x20000000), blurRadius: 8.0, offset: Offset(0, 4)),
  BoxShadow(color: Colors.red, blurRadius: 8.0, offset: Offset(0, 4)),
];
